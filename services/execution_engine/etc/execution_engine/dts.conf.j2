dts {
  db {
    host = "127.0.0.1"
    port = 27017
    name = "ccc"
    collection = "dts"
  }
  http {
    host = "{{ DTS_INTERFACE | default("0.0.0.0") }}"
    port = {{ DTS_PORT | default(9510) }}
  }
}

akka {
  actor {
    debug {
      fsm = on
    }
  }
  loggers = ["akka.event.slf4j.Slf4jLogger"]
  loglevel = DEBUG
}
